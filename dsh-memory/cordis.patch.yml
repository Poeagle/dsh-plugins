# dsh-memory bundle patch. The settings row registers the HTTP route for memory
# management; the index row is the existing memory tool + review fork.
# The browser half is discovered through the package.json dsh.client manifest.
- insert:
    - id: memory-settings
      name: dsh-memory/settings
      config: {}